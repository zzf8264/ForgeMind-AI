# Scripts

- `seed_demo_data.py`: prints deterministic demo rows for agent runs and token usage.
- `create_demo_workspace.py`: creates a small source repository for RAG indexing experiments.
- `run_local.sh`: starts the Docker Compose stack from `infra/`.
